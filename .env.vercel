NODE_ENV=production

# These are placeholders and should be set in the Vercel dashboard
MONGODB_URI=mongodb+srv://your-mongodb-uri
SESSION_SECRET=your-session-secret

# Frontend environment variables (must be prefixed with VITE_)
VITE_API_URL=/api