# Session secret for cookie encryption
SESSION_SECRET=your_session_secret_key_here

# MongoDB connection string
# Replace <username>, <password>, <cluster-url> with your MongoDB Atlas information  
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/url-shortener?retryWrites=true&w=majority

# Optional: Set to true to enable debug logging
DEBUG=false